import { City } from '../entities/city';
import { Storage } from '@ionic/storage';

export class HistoryCityService {
//   constructor(
//     private store: Storage
//   ) {}

//   async isEmpty () : Promise<boolean> {
//     return (await this.store.length() == 0)? true : false;
//   } 

//   async saveByCity (cityKey: string, city: City) {
//     await this.store.set(cityKey, city);
//   }

//   async loadByCityies (): Promise<City[]> {
    
//     let cities: City[];
//     this.store.forEach(async (key, value, index) => {
//         const city = await this.store.get(key);
//         cities.push(city);
//     });

//     return cities;
//   }
}